name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend:
    name: Backend Lint & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/backend
    services:
      postgres:
        image: pgvector/pgvector:pg18
        env:
          POSTGRES_USER: aitown
          POSTGRES_PASSWORD: aitown_dev
          POSTGRES_DB: aitown
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql+asyncpg://aitown:aitown_dev@localhost:5432/aitown
      REDIS_URL: redis://localhost:6379/0
      JWT_SECRET: ci-test-secret-at-least-32-chars-long
      OPENAI_API_KEY: sk-test-placeholder
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: ci-test-password-not-default
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - name: Set up Python
        run: uv python install 3.13
      - name: Install dependencies
        run: uv sync --frozen
      - name: Lint with ruff
        run: uv run ruff check src/ tests/
      - name: Format check with ruff
        run: uv run ruff format --check src/ tests/
      - name: Type check with mypy
        run: uv run mypy src/ tests/
      - name: Run tests
        run: uv run pytest --tb=short -q
        env:
          DATABASE_URL: postgresql+asyncpg://aitown:aitown_dev@localhost:5432/aitown
          REDIS_URL: redis://localhost:6379/0
          IT_PG_HOST: localhost
          IT_PG_PORT: "5432"
          IT_PG_USER: aitown
          IT_PG_PASSWORD: aitown_dev
          IT_REDIS_URL: redis://localhost:6379/15
      - name: OpenAPI contract regeneration guard
        # R5-L18：路由漂移必须在后端侧拦截——前端已有 gen:api 守卫，
        # 但若 openapi.json 本身未随路由变更重新导出，前端守卫永远比对不出差异
        run: |
          uv run python scripts/export_openapi.py
          git diff --exit-code -- openapi.json || (echo "openapi.json 与路由定义不同步：请运行 uv run python scripts/export_openapi.py 并提交" && exit 1)

  frontend:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/frontend
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          # 与 packages/frontend/package.json 的 packageManager 字段保持一致
          version: 11
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: '**/pnpm-lock.yaml'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint with oxlint
        run: pnpm run lint
      - name: Line limit guard (P3-5)
        run: node scripts/check-loc.mjs
      - name: Type check
        run: pnpm run typecheck
      - name: API type contract guard
        run: |
          pnpm gen:api
          git diff --exit-code -- src/types/api-generated.d.ts || (echo "后端契约变更未同步：请运行 pnpm gen:api 并提交" && exit 1)
      - name: Unit tests
        run: pnpm test -- --run
      - name: Build
        run: pnpm run build

  deploy-smoke:
    # R4-H4：部署冒烟——此前 CI 零镜像构建/零运行时验证，
    # c387487（镜像不可构建、后端容器从未成功启动）与 900c73d（nginx 运行时 502）
    # 两类事故只能靠人肉发现，本作业关闭该复发通道
    name: Deploy Smoke (build & boot)
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    env:
      POSTGRES_PASSWORD: smoke-test-password
      REDIS_PASSWORD: smoke-test-password
      ENVIRONMENT: development
      # compose 对所有服务（含未激活 profile 的 observability 层）做插值，
      # grafana 的 ${GRAFANA_ADMIN_PASSWORD:?} 缺失会让 config 阶段直接失败
      GRAFANA_ADMIN_PASSWORD: ci-smoke-grafana
      # langfuse-web/worker 的 ${LANGFUSE_*:?} 同理在插值期强制必填
      LANGFUSE_NEXTAUTH_SECRET: ci-smoke-langfuse-nextauth
      LANGFUSE_SALT_KEY: ci-smoke-langfuse-salt
      LANGFUSE_ENCRYPTION_KEY: ci-smoke-langfuse-enc
    steps:
      - uses: actions/checkout@v5
      - name: Validate compose config
        # R5-H1：叠加 CI 覆盖层注入 Settings 必填字段（OPENAI_API_KEY/JWT_SECRET 等），
        # 否则 backend 容器导入期即崩溃，health wait 必然超时
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml config -q
      - name: Build images
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml build backend frontend
      - name: Boot core stack
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d postgres redis backend frontend
      - name: Wait for backend health (direct)
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8001/health > /dev/null; then echo "backend healthy"; exit 0; fi
            sleep 2
          done
          echo "backend failed to become healthy" && docker compose -f docker-compose.yml -f docker-compose.ci.yml logs backend --tail 100 && exit 1
      - name: Wait for frontend health (through nginx proxy)
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:80/health > /dev/null; then echo "frontend proxy healthy"; exit 0; fi
            sleep 2
          done
          echo "nginx proxy failed" && docker compose -f docker-compose.yml -f docker-compose.ci.yml logs frontend --tail 100 && exit 1
      - name: Tear down
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml down -v -t 30
