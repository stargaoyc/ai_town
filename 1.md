二次元AI小镇陪伴智能体 — 总体设计文档
一、项目概述
1.1 项目背景
随着大语言模型技术的快速发展，AI陪伴产品正从简单的对话机器人向具备自主推理、工具调用和长期记忆能力的智能体（Agent）演进。本项目旨在构建一个由LLM驱动的多智能体虚拟小镇，让AI角色拥有独立的记忆、反思、规划与社交能力，在持续运行的虚拟世界中自主生活。

核心理念：不做“随叫随到的AI助手”，而是做一群有自己生活的“人” 。用户的每一次对话，都来自角色在小镇中真实经历的事件，而非临时生成的人设文本。

1.2 项目目标
目标	说明
多角色共居	支持10-50个AI角色同时在小镇中生活、决策、交互
世界持续运行	世界状态推进不依赖用户消息，角色在用户不在时依然生活
记忆与演化	角色拥有记忆流、反思能力和长期规划，行为长期一致且可演化
可插拔能力	功能模块（代码执行、搜索、绘图等）可动态启用/禁用，热插拔
全链路可观测	每个决策周期可追踪、可审计、可调试
多端触达	支持Web Dashboard、QQ、飞书等多渠道交互
1.3 设计原则
原则	说明
状态驱动	LLM是决策和生成能力，不是状态真相源；所有状态变更由代码执行
事实优先	所有可追溯事实必须落到行为记录或明确的状态字段中
闭环演化	行为沉淀为记忆 → 记忆影响未来决策 → 形成可追溯的生活轨迹
模块解耦	核心引擎与功能模块分离，模块可独立开关、独立升级
可观测性	埋点即契约，所有关键路径必须有Trace覆盖
二、总体架构设计
2.1 分层架构
text
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户接入层 (Access Layer)                         │
│          Web Dashboard  │   QQ (OneBot)  │   飞书 (Lark)   │  (未来扩展)   │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────────────┐
│                          消息服务层 (Messaging Layer)                       │
│         消息标准化  │  会话上下文管理  │  回复生成  │  主动推送调度          │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────────────┐
│                        世界引擎层 (World Engine Layer)                      │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │   世界状态推进       │  │   角色行为推进       │  │   多智能体调度       │ │
│  │  (World Tick)       │  │  (Character Tick)   │  │  (Multi-Agent)      │ │
│  │ 时间/天气/场景/资源  │  │ Action决策/执行/记录 │  │ 角色间通信/事件广播  │ │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘ │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────────────┐
│                        能力层 (Capability Layer)                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 记忆系统  │ │ 反思系统  │ │ 规划系统  │ │ 情感引擎  │ │  模块管理器      │ │
│  │(向量检索) │ │(周期总结) │ │(目标分解) │ │(角色人设) │ │(开关/生命周期)   │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ ────────┘ │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    MCP 工具调用层 (标准化接口)                         │ │
│  │    代码执行  │  网页搜索  │  天气查询  │  商店模拟  │  第三方API      │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────────────┐
│                        基础设施层 (Infrastructure Layer)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐ │
│  │ PostgreSQL  │  │    Redis    │  │  向量数据库  │  │  对象存储         │ │
│  │ (+pgvector) │  │ (缓存/队列) │  │  (Chroma)   │  │  (MinIO/S3)       │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └───────────────────┘ │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                可观测性 (OpenTelemetry + Langfuse)                     │ │
│  │        链路追踪(Traces)  │  指标(Metrics)  │  日志(Logs)              │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
2.2 数据流闭环
text
┌─────────────────────────────────────────────────────────────────────────────┐
│                        核心闭环: State → Decision → Action → Update         │
│                                                                             │
│   ┌───────────────┐     ┌───────────────┐     ┌───────────────┐           │
│   │   State       │────▶│   Decision    │────▶│   Action      │           │
│   │  角色状态     │     │  LLM在候选    │     │  Executor     │           │
│   │  世界状态     │     │  Action中决策  │     │  执行副作用   │           │
│   │  历史经历     │     │               │     │               │           │
│   └───────▲───────┘     └───────────────┘     └───────┬───────┘           │
│           │                                            │                    │
│           │            ┌───────────────┐              │                    │
│           └────────────│   Update     │◀──────────────┘                    │
│                        │  状态变更    │                                      │
│                        │  记忆沉淀    │                                      │
│                        └───────────────┘                                    │
└─────────────────────────────────────────────────────────────────────────────┘
2.3 技术栈总览
层次	类别	技术选型
后端核心	Agent框架	LangGraph (Python)
Web框架	FastAPI
包管理	uv / Poetry
异步驱动	motor (MongoDB) + redis-py (Redis)
前端	UI框架	React 19
路由	TanStack Router
状态管理	TanStack Query + Zustand
构建工具	Vite 8
组件库	shadcn/ui + Tailwind CSS v4
包管理	pnpm 11
数据存储	主数据库	PostgreSQL 17 (+ pgvector)
缓存/实时状态	Redis 7.4+
向量数据库	Chroma (开发) / Milvus (生产)
对象存储	MinIO / AWS S3
中间件	消息队列	Redis Streams
工具调用	MCP协议	官方 mcp Python SDK
可观测性	LLM追踪	Langfuse
链路追踪	OpenTelemetry + Jaeger
指标	Prometheus + Grafana
三、后端详细设计
3.1 世界引擎 (World Engine)
世界引擎是系统的核心调度器，由两个独立的异步循环组成。

3.1.1 世界状态推进 (World Tick)
职责：推进与角色无关的全局状态。

子功能	说明	更新频率
时间推进	虚拟时钟按Tick步进（如每30秒推进10分钟）	每Tick
天气系统	晴天/多云/阴雨/雪/大风，影响角色行为选择	每60 Tick
场景状态	各地点（咖啡店、学校、公园）开放/关闭/拥挤度	每Tick
资源循环	城镇资源（食物、能源）动态增减	每Tick
节日系统	特殊日期的触发与广播	按日期
设计约束：World Tick 不依赖用户消息——世界在用户不在时依然运转。

3.1.2 角色行为推进 (Character Tick)
每个角色独立执行以下闭环：

text
① 感知环境
   ├─ 读取角色状态（位置/精力/情绪/当前行为）
   ├─ 读取世界状态（时间/天气/场景）
   ├─ 读取周围角色（同位置的其他角色）
   └─ 记忆检索（从向量库检索Top-K相关记忆）
        ↓
② 候选Action过滤
   └─ 遍历所有Action，检查precondition，生成候选列表
        ↓
③ LLM决策
   ├─ 输入: 角色状态 + 世界状态 + 候选列表 + 检索到的记忆
   ├─ 模型: strong 类型（复杂决策）
   └─ 输出: 结构化决策 { action, reason, params, duration }
        ↓
④ Action执行
   ├─ 更新Redis状态（位置/精力/行为）
   ├─ 写入MongoDB行为记录（ActionRecord）
   └─ 生成MemoryEpisode存入向量库
        ↓
⑤ 记忆沉淀与反思触发
   ├─ 检查是否触发反思（如记忆数量达到阈值）
   └─ 检查是否需要调整计划
3.2 Action系统
Action是角色的原子行为单元。

3.2.1 Action定义
python
@dataclass
class Action:
    id: str                          # 唯一标识
    name: str                        # 显示名称
    category: ActionCategory         # MOVE / LIFE / WORK / SOCIAL / TOOL
    precondition: Callable[[State], bool]  # 前置条件判断
    executor: Callable[[State, dict], State] # 执行逻辑
    duration_minutes: int            # 预计耗时
    energy_cost: int                 # 精力消耗（-10 ~ +10）
    social_impact: int               # 社交影响（-5 ~ +5）
3.2.2 Action分类与示例
分类	示例Action	Precondition	副作用
移动	move_home_to_school	在家 && 工作日 && 8:00-9:00	location→学校
move_to_cafe	任意 && cafe开放	location→咖啡店
生活	eat_meal	饥饿度>70 && 有食物	hunger-30, energy+10
sleep	精力<30 && 在家	energy+50
工作	study	在学校 && 9:00-17:00	knowledge+5
work_parttime	在咖啡店 && 有排班	money+10
社交	chat_with	同位置有其他角色	relationship+2
工具	search_info	无限制	调用MCP搜索
3.3 模块管理器 (Module Manager)
模块管理器是系统的可插拔能力中枢，负责所有功能模块的注册、开关控制和生命周期管理。

3.3.1 模块类型
模块类型	说明	示例
MCP模块	通过MCP协议调用的外部服务	代码执行、网页搜索、天气查询
本地模块	内联函数，毫秒级响应	情感分析、记忆检索、本地工具
Skill模块	多步骤复杂工作流	数据分析报告生成、多轮调研
3.3.2 模块生命周期
text
REGISTERED(已注册) → ENABLED(已启用) → 运行中
       ↓                  ↓
    DISABLED(已禁用)    ERROR(错误)
注册：系统启动时扫描并注册所有可用模块

启用：检查依赖→健康检查→加载配置→标记可用

调用：Agent决策后通过统一接口调用

禁用：检查反向依赖→清理资源→标记不可用

卸载：完全移除模块（热插拔）

3.3.3 开关控制方式
方式	说明	适用场景
配置文件	config.yaml 中配置 enabled: true/false	运维管理，重启生效
运行时API	POST /modules/{name}/enable	灰度测试、紧急禁用
Agent自适应	LLM根据上下文决策是否使用	智能降级、自主选择
3.4 记忆系统
记忆系统让角色拥有“过去”，是长期一致性的基础。

3.4.1 三层记忆架构
text
┌─────────────────────────────────────────────────────────────────┐
│                    1. 原始记忆 (Memory Stream)                  │
│    所有行为记录的原始日志，存入向量数据库，支持语义检索          │
│    [昨天去了咖啡店] [周一早上迟到了] [认识了新朋友]             │
└─────────────────────────┬───────────────────────────────────────┘
                          │ 定期总结 (每N条触发)
┌─────────────────────────▼───────────────────────────────────────┐
│                    2. 反思总结 (Reflection)                     │
│    对大量原始记忆的归纳提炼，形成高层次自我认知                  │
│    ["我习惯早睡早起", "我对社交有点焦虑", "我喜欢雨天"]         │
└─────────────────────────┬───────────────────────────────────────┘
                          │ 影响
┌─────────────────────────▼───────────────────────────────────────┐
│                    3. 长期规划 (Planning)                       │
│    基于性格和反思生成的长期目标与每日计划                        │
│    [三个月内学会咖啡拉花] [明天8:00去学校]                      │
└─────────────────────────────────────────────────────────────────┘
3.4.2 记忆检索流程
text
用户提问/决策触发 → 生成查询向量 → 向量检索Top-K → 按时间衰减重排序 → 注入LLM上下文
3.4.3 反思触发机制
触发条件	说明
数量阈值	每新增20条记忆触发一次反思
时间阈值	每日固定时间（如晚上22:00）
事件触发	关键事件发生后（如关系变化、重大决策）
3.5 MCP工具调用层
3.5.1 MCP架构图
text
┌─────────────────────────────────────────────────────────────────┐
│                   世界引擎 (MCP Client)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │ 工具发现     │  │ 工具调用    │  │ 结果处理与重试          │ │
│  │ (ListTools) │  │ (CallTool) │  │ (超时/降级/熔断)        │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
                              │ JSON-RPC 2.0 / SSE
┌─────────────────────────────▼───────────────────────────────────┐
│                    MCP Server Cluster                            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │
│  │ 代码执行     │ │ 网页搜索     │ │ 天气查询     │           │
│  │ Server       │ │ Server       │ │ Server       │           │
│  └──────────────┘ └──────────────┘ └──────────────┘           │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │
│  │ 商店模拟     │ │ 知识库查询   │ │ 第三方API    │           │
│  │ Server       │ │ Server       │ │ Server       │           │
│  └──────────────┘ └──────────────┘ └──────────────┘           │
└─────────────────────────────────────────────────────────────────┘
3.5.2 MCP集成方式
python
# MCP Server 注册工具示例
from mcp.server import Server, tool

server = Server("code-executor")

@server.tool()
async def run_python(code: str) -> dict:
    """在沙箱中执行Python代码"""
    # 实际执行逻辑
    return {"output": "...", "error": None}

# MCP Client 调用示例
async def call_mcp_tool(server_url: str, tool_name: str, params: dict):
    async with mcp.ClientSession(server_url) as session:
        # 动态发现工具
        tools = await session.list_tools()
        # 调用工具
        result = await session.call_tool(tool_name, params)
        return result
3.6 消息服务
3.6.1 多平台接入
平台	协议	实现方式
QQ	OneBot v12	aiocqhttp 异步客户端
飞书	Lark OpenAPI	lark-python SDK
Web	WebSocket	FastAPI WebSocket端点
3.6.2 消息处理流程
text
平台消息 → 标准化(统一Message格式) → 权限校验(白名单) → 构造上下文(含角色状态) 
  → LLM生成回复 → 发送 → 记录对话历史
3.6.3 主动推送机制
角色基于 proactiveShareIntent 主动向用户发送消息：

text
Action执行完成 → 检查 proactiveShareIntent.shouldShare = true 
  → 生成分享内容 → 推送至消息队列 → 消息服务发送
四、前端详细设计
4.1 页面结构
text
┌─────────────────────────────────────────────────────────────────────┐
│  Sidebar (导航)   │   Content Area                                 │
│  ─────────────    │                                               │
│  📊 仪表盘        │                                               │
│  🏘️ 小镇管理     │                                               │
│  👤 角色管理      │                                               │
│  🧩 模块管理      │                                               │
│  💬 会话监控      │                                               │
│  📈 可观测性      │                                               │
│  ⚙️ 系统设置      │                                               │
└─────────────────────────────────────────────────────────────────────┘
4.2 页面详细功能
页面	核心功能	关键组件
仪表盘	总览卡片、趋势图、最近事件流	Recharts、滚动列表
小镇管理	世界状态控制(时间/天气)、场景地图、事件广播	可视化地图、控制面板
角色管理	角色列表/创建/编辑、实时状态卡片、关系图谱	表格+表单、关系图
模块管理	模块列表(类型/状态/依赖)、开关控制、MCP Server管理	表格、开关组件、日志查看器
会话监控	多渠道会话列表、对话详情、人工干预	聊天界面、消息编辑器
可观测性	调用链追踪、日志查询、指标图表、告警配置	Trace视图、日志搜索、图表面板
系统设置	模型配置、Prompt编辑、权限管理	表单、代码编辑器
4.3 目录结构
text
packages/frontend/
├── index.html
├── package.json
├── pnpm-workspace.yaml
├── vite.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── src/
│   ├── main.tsx                    # 入口文件
│   ├── App.tsx                     # 根组件
│   ├── routes/                     # TanStack Router 路由定义
│   │   ├── __root.tsx
│   │   ├── dashboard/
│   │   ├── town/
│   │   ├── characters/
│   │   ├── modules/
│   │   ├── conversations/
│   │   ├── observability/
│   │   └── settings/
│   ├── components/
│   │   ├── ui/                     # shadcn/ui 基础组件
│   │   ├── layout/                 # 布局组件 (Sidebar, Header)
│   │   ├── dashboard/
│   │   ├── town/
│   │   ├── characters/
│   │   ├── modules/
│   │   └── shared/                 # 通用组件 (Loading, ErrorBoundary)
│   ├── stores/                     # Zustand 状态管理
│   │   ├── app-store.ts
│   │   ├── character-store.ts
│   │   ├── module-store.ts
│   │   └── websocket-store.ts
│   ├── api/                        # OpenAPI 生成的客户端
│   │   ├── client.ts
│   │   ├── types.ts
│   │   └── hooks/                  # TanStack Query hooks
│   ├── hooks/                      # 自定义Hooks
│   │   ├── use-websocket.ts
│   │   ├── use-toast.ts
│   │   └── use-debounce.ts
│   ├── lib/                        # 工具函数
│   │   ├── utils.ts
│   │   └── constants.ts
│   ├── styles/
│   │   └── globals.css
│   └── types/                      # 共享类型定义
│       └── index.ts
├── public/
├── tests/
│   ├── unit/
│   └── e2e/
└── storybook/
4.4 实时数据流设计
text
┌─────────────┐      WebSocket      ┌─────────────────────────────┐
│  后端服务   │ ──────────────────▶ │  前端状态管理               │
│  (FastAPI)  │    SSE/WebSocket    │  ┌─────────────────────────┐│
└─────────────┘                     │  │  Zustand Store (全局)   ││
                                    │  │  - 角色实时状态         ││
                                    │  │  - 世界状态             ││
                                    │  │  - 模块状态             ││
                                    │  └─────────────────────────┘│
                                    │              │              │
                                    │              ▼              │
                                    │  ┌─────────────────────────┐│
                                    │  │  TanStack Query         ││
                                    │  │  (服务端缓存/重试)      ││
                                    │  └─────────────────────────┘│
                                    └─────────────────────────────┘
五、数据模型设计
5.1 核心实体关系图
text
┌─────────────┐     ┌─────────────┐     ┌─────────────────┐
│  Character  │────▶│  State      │     │  ActionRecord   │
│  (MongoDB)  │     │  (Redis)    │     │  (MongoDB)      │
└─────────────┘     └─────────────┘     └─────────────────┘
       │                                          │
       │                                          │
       ▼                                          ▼
┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Memory     │     │  Reflection     │     │  Plan           │
│  (向量库)   │     │  (MongoDB)      │     │  (MongoDB)      │
└─────────────┘     └─────────────────┘     └─────────────────┘
       │
       │
       ▼
┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Relation   │     │  Message        │     │  WorldState     │
│  (MongoDB)  │     │  (MongoDB)      │     │  (Redis)        │
└─────────────┘     └─────────────────┘     └─────────────────┘
5.2 详细数据模型
Character (MongoDB)
typescript
interface Character {
  id: string;
  name: string;
  age: number;
  occupation: string;
  personality: string[];      // ["开朗", "细心", "有点社恐"]
  traits: Record<string, any>; // 自定义属性
  backstory: string;           // 背景故事
  avatar_url: string;
  created_at: Date;
  updated_at: Date;
}
CharacterState (Redis)
typescript
interface CharacterState {
  character_id: string;
  location: string;           // 当前位置ID
  current_action: string;     // 当前行为ID
  action_started_at: number;  // 行为开始时间戳
  energy: number;             // 0-100
  hunger: number;             // 0-100
  mood: string;               // happy/sad/tired/excited
  last_updated: number;
}
ActionRecord (MongoDB)
typescript
interface ActionRecord {
  id: string;
  character_id: string;
  action_id: string;
  action_name: string;
  params: Record<string, any>;
  reason: string;              // LLM给出的选择理由
  result: string;              // 执行结果描述
  duration_minutes: number;
  created_at: Date;
  location: string;
  related_characters?: string[];
}
MemoryEpisode (向量数据库)
typescript
interface MemoryEpisode {
  id: string;
  character_id: string;
  content: string;             // 自然语言描述
  embedding: number[];         // 向量
  importance: number;          // 1-10 重要程度
  timestamp: Date;
  metadata: {
    action_id?: string;
    location?: string;
    related_characters?: string[];
  };
}
ModuleConfig (PostgreSQL)
typescript
interface ModuleConfig {
  id: string;
  name: string;
  type: 'mcp' | 'local' | 'skill';
  enabled: boolean;
  config: Record<string, any>;  // 模块特定配置
  dependencies: string[];       // 依赖的模块名称列表
  mcp_server_url?: string;      // 如果是MCP类型
  health_check_status: 'healthy' | 'unhealthy' | 'unknown';
  last_check_at: Date;
}
WorldState (Redis)
typescript
interface WorldState {
  current_time: number;        // 虚拟时间戳
  weather: 'sunny' | 'cloudy' | 'rainy' | 'snowy' | 'windy';
  temperature: number;
  locations: Record<string, {
    open: boolean;
    crowdedness: number;      // 0-100
    visitors: string[];        // 在其中的角色ID列表
  }>;
  resources: Record<string, number>;
  active_events: Array<{ id: string; name: string; description: string }>;
}
六、API设计
6.1 RESTful API (FastAPI)
端点	方法	说明
/api/v1/characters	GET/POST	角色列表/创建
/api/v1/characters/{id}	GET/PUT/DELETE	角色详情/更新/删除
/api/v1/characters/{id}/state	GET	角色实时状态
/api/v1/characters/{id}/memories	GET	角色记忆列表
/api/v1/characters/{id}/actions	GET	角色行为历史
/api/v1/world/state	GET	世界状态
/api/v1/world/weather	PUT	手动设置天气
/api/v1/world/time	PUT	手动调整时间
/api/v1/modules	GET/POST	模块列表/注册
/api/v1/modules/{name}/enable	POST	启用模块
/api/v1/modules/{name}/disable	POST	禁用模块
/api/v1/modules/{name}/health	GET	模块健康检查
/api/v1/mcp/servers	GET/POST	MCP Server管理
/api/v1/mcp/tools	GET	所有可用工具列表
/api/v1/conversations	GET	会话列表
/api/v1/conversations/{id}/messages	GET	会话消息历史
/api/v1/conversations/{id}/intervene	POST	人工干预插入消息
/api/v1/observability/traces	GET	链路查询
/api/v1/observability/logs	GET	日志查询
/api/v1/observability/metrics	GET	指标数据
/api/v1/settings/models	GET/PUT	模型配置
/api/v1/settings/prompts	GET/PUT	Prompt管理
6.2 WebSocket/SSE
端点	说明
/ws/dashboard	仪表盘实时数据推送
/ws/characters/{id}	特定角色状态推送
/ws/modules	模块状态变更推送
/sse/traces	链路追踪实时流
七、可观测性设计
7.1 埋点覆盖矩阵
埋点位置	Span名称	关键属性
World Tick	world.tick	tick_id, weather, time_advance
Character Tick	character.tick	character_id, tick_duration
LLM调用	llm.generate	model_name, tokens, temperature, cost
Action决策	action.decision	character_id, action_name, reason
Action执行	action.execute	action_id, duration, success
记忆检索	memory.retrieve	character_id, query, top_k
反思生成	memory.reflect	character_id, memory_count
MCP工具调用	mcp.tool.call	tool_name, server_url, latency
消息处理	message.process	platform, session_id, response_time
模块操作	module.{enable|disable|call}	module_name, status
7.2 可观测性架构
text
┌─────────────────────────────────────────────────────────────────┐
│                      应用 (Python/LangGraph)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐│
│  │  OTel SDK   │  │  Langfuse   │  │  Python Logging         ││
│  │  (自动埋点)  │  │  SDK (LLM)  │  │  (结构化日志)           ││
│  └─────────────┘  └─────────────┘  └─────────────────────────┘│
└─────────────────────────────┬───────────────────────────────────┘
                              │ OTLP / HTTP
┌─────────────────────────────▼───────────────────────────────────┐
│                        收集器层                                   │
│              OpenTelemetry Collector (OTel Col)                  │
│         接收 → 处理 → 导出 (批处理/采样/过滤)                    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼───────┐    ┌───────▼───────┐    ┌───────▼───────┐
│   Traces      │    │   Metrics     │    │    Logs       │
│   Jaeger      │    │  Prometheus   │    │   Loki        │
│   / Langfuse  │    │  + Grafana   │    │   (可选)      │
└───────────────┘    └───────────────┘    └───────────────┘
7.3 关键指标
指标名	类型	说明	告警阈值
character_tick_duration	Histogram	角色Tick耗时	p95 > 5s
llm_call_duration	Histogram	LLM调用延迟	p95 > 10s
llm_token_usage	Counter	Token消耗	日环比 > 50%
mcp_tool_error_rate	Gauge	MCP工具错误率	> 5%
action_execution_failed	Counter	Action执行失败	> 10/h
module_unhealthy	Gauge	不健康模块数	> 0
active_characters	Gauge	活跃角色数	—
八、部署与运维
8.1 部署架构
text
┌─────────────────────────────────────────────────────────────────┐
│                        用户访问                                 │
│          Web Browser  │  QQ  │  飞书                           │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                    Nginx (反向代理/负载均衡)                     │
│             静态资源缓存 / SSL终止 / 路由分发                    │
└──────────┬─────────────────────────────┬───────────────────────┘
           │                             │
┌──────────▼──────────┐     ┌────────────▼──────────────┐
│   前端 (Vite build) │     │     后端 (FastAPI)         │
│   静态文件/CDN      │     │   World Engine + LangGraph │
└─────────────────────┘     └────────────┬──────────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
           ┌────────▼────────┐  ┌────────▼────────┐  ┌──────▼──────┐
           │   PostgreSQL    │  │     Redis       │  │  向量数据库  │
           │   (+pgvector)   │  │  (缓存/队列)    │  │  (Chroma)   │
           └─────────────────┘  └─────────────────┘  └─────────────┘
                    │
           ┌────────▼────────┐
           │   MCP Servers   │
           │  (多进程/Docker) │
           └─────────────────┘
8.2 容器化部署
dockerfile
# 后端 Dockerfile
FROM python:3.12-slim
RUN pip install uv
WORKDIR /app
COPY pyproject.toml .
RUN uv sync
COPY . .
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]

# 前端 Dockerfile
FROM node:22-alpine AS builder
RUN npm install -g pnpm
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install
COPY . .
RUN pnpm build
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
8.3 环境变量清单
bash
# 数据库
DATABASE_URL=postgresql://user:pass@localhost:5432/ai_town
REDIS_URL=redis://localhost:6379/0

# LLM配置
OPENAI_API_KEY=xxx
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_CHAT=gpt-4o-mini
MODEL_STRONG=gpt-4o
MODEL_FLASH=gpt-3.5-turbo

# MCP Servers
MCP_CODE_SERVER=http://localhost:8001
MCP_SEARCH_SERVER=http://localhost:8002

# 可观测性
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
LANGFUSE_PUBLIC_KEY=xxx
LANGFUSE_SECRET_KEY=xxx

# 消息平台
ONE_BOT_WS_URL=ws://localhost:6700
LARK_APP_ID=xxx
LARK_APP_SECRET=xxx
九、项目结构 (Monorepo)
text
ai-town/
├── packages/
│   ├── backend/                      # Python 后端
│   │   ├── src/
│   │   │   ├── core/                 # 核心引擎
│   │   │   │   ├── world_engine.py
│   │   │   │   ├── character_tick.py
│   │   │   │   └── action_system.py
│   │   │   ├── agents/               # 角色实现
│   │   │   ├── memory/               # 记忆系统
│   │   │   ├── modules/              # 模块管理器
│   │   │   ├── tools/                # MCP集成
│   │   │   ├── messaging/            # 消息服务
│   │   │   ├── api/                  # FastAPI路由
│   │   │   ├── models/               # 数据模型
│   │   │   ├── observability/        # OTel配置
│   │   │   └── main.py
│   │   ├── pyproject.toml
│   │   ├── Dockerfile
│   │   └── tests/
│   │
│   ├── frontend/                     # React 前端
│   │   ├── src/                      # (见4.3目录结构)
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── Dockerfile
│   │   └── tests/
│   │
│   ├── mcp-servers/                  # MCP Server集合
│   │   ├── code-executor/
│   │   ├── web-search/
│   │   └── weather/
│   │
│   └── shared/                       # 前后端共享
│       ├── types/                    # TypeScript类型定义
│       └── openapi/                  # OpenAPI规范
│
├── docker-compose.yml                # 完整部署编排
├── config.yaml                       # 主配置文件
├── .env.example
└── README.md

角色卡、提示词也可以配置